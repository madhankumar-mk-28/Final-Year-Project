# ── PyTorch CPU-only build (saves ~1.5 GB vs CUDA wheel — critical for Render free tier)
--extra-index-url https://download.pytorch.org/whl/cpu

# ── Web framework
flask>=3.0.0,<4.0
flask-cors>=4.0.0,<5.0
gunicorn>=21.2.0,<22.0

# ── ML / embeddings
torch>=2.2.0
sentence-transformers>=3.0.0,<4.0

# ── PDF parsing
pdfplumber>=0.11.0,<1.0
PyMuPDF>=1.24.0,<2.0

# ── Numerics (sentence-transformers pulls numpy transitively, pin to avoid ABI mismatch)
numpy>=1.26.0,<3.0

# ── Werkzeug is a flask dep; pin to avoid breaking changes
Werkzeug>=3.0.0,<4.0
